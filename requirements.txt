Flask==2.3.2           # Flask web framework
Pillow==10.1.1         # For image processing
Werkzeug==2.3.2        # Flask's underlying WSGI library
gunicorn==21.2.0       # Production-ready server for deploying Flask apps
